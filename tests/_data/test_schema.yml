volumes:
  uploads:
    attributes:
      keyId: original_key_id
      bucket: '%S3_BUCKET%'
users:
  settings:
    requireEmailVerification: true
    allowPublicRegistration: false
    defaultGroup: null
    photoSubpath: profile
